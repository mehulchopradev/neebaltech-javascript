// initialize a variable n - 50
// print all even nos till n

const n = 30;

// while loop
/* let i = 0;

while (i <= n) {
  if (i % 2 === 0) {
    console.log(i);
  }

  i += 1; 

  // regular labels like break and continue can be used
} */

// for loop
for (let i = 0; i <= n; i++) {
  if (i % 2 === 0) {
    console.log(i);
  }
  // regular labels like break and continue can be used
}